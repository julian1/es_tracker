
md5 digest of content. could be useful for comparing/merging event streams.

prod=> select id, t, msg, md5( content::varchar) as digest, length( content::varchar) from events limit 10;

---

it would be really nice to have a migrations table that recorded what
has run and what hasn't


